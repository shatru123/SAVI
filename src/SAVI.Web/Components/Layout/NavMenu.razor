<div class="savi-glass-panel savi-hud-bracket" style="padding: 16px; height: 100%; display: flex; flex-direction: column; gap: 14px;">
    <!-- Logo & Brand -->
    <div style="display: flex; align-items: center; gap: 10px; border-bottom: 1px solid var(--savi-border-subtle); padding-bottom: 14px;">
        <div style="width: 14px; height: 14px; border-radius: 50%; background: var(--savi-cyan); box-shadow: 0 0 10px var(--savi-cyan);"></div>
        <div>
            <div class="savi-glow-text" style="font-weight: 700; font-size: 1.05rem; letter-spacing: 2px;">SAVI</div>
            <div style="font-size: 0.68rem; color: var(--savi-text-dim); letter-spacing: 0.5px;">VIRTUAL INTELLIGENCE</div>
        </div>
    </div>

    <!-- Navigation links -->
    <nav style="display: flex; flex-direction: column; gap: 6px; flex: 1;">
        <NavLink class="savi-nav-item" href="" Match="NavLinkMatch.All">
            <span class="savi-icon">◈</span> Command Deck
        </NavLink>
        <NavLink class="savi-nav-item" href="memory">
            <span class="savi-icon">🧠</span> Long-Term Memory
        </NavLink>
        <NavLink class="savi-nav-item" href="tasks">
            <span class="savi-icon">⚙</span> Task Matrix
        </NavLink>
        <NavLink class="savi-nav-item" href="providers">
            <span class="savi-icon">📡</span> Provider Registry
        </NavLink>
        <NavLink class="savi-nav-item" href="audit">
            <span class="savi-icon">🛡</span> Security & Audit
        </NavLink>
        <NavLink class="savi-nav-item" href="settings">
            <span class="savi-icon">⚙</span> Configuration
        </NavLink>
    </nav>

    <!-- Bottom System Status -->
    <div style="border-top: 1px solid var(--savi-border-subtle); padding-top: 12px; font-size: 0.72rem; color: var(--savi-text-secondary);">
        <div style="display: flex; align-items: center; gap: 6px; margin-bottom: 4px;">
            <span style="color: var(--savi-success); font-size: 0.7rem;">●</span> ZERO-COST CORE
        </div>
        <div style="color: var(--savi-text-dim);">No paid APIs active</div>
    </div>
</div>

<style>
.savi-nav-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 14px;
    color: var(--savi-text-secondary);
    text-decoration: none;
    font-size: 0.86rem;
    border-radius: 4px;
    border: 1px solid transparent;
    transition: all 0.2s ease;
}
.savi-nav-item:hover, .savi-nav-item.active {
    color: var(--savi-cyan);
    background: rgba(0, 240, 255, 0.08);
    border-color: var(--savi-border-subtle);
    box-shadow: 0 0 10px rgba(0, 240, 255, 0.15);
}
.savi-icon {
    font-size: 1rem;
}
</style>
